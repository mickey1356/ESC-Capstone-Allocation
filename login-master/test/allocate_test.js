const {Allocator, Path} = require("../src/map/allocate.js");
const SPACE = require("./space.js");


test("allocator gets correct dimensions", () => {
    allocator = new Allocator();
    allocator.load_grid(SPACE);
    expect()
});
